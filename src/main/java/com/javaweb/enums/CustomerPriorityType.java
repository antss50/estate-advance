package com.javaweb.enums;

public enum CustomerPriorityType {
    DEFAULT,    // Mặc định
    SAVING,     // Tiết kiệm (ưu tiên giá)
    CONVENIENT, // Tiện lợi (ưu tiên vị trí)
    SPACIOUS    // Không gian (ưu tiên diện tích)
}